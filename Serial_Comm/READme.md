# Python-based GUI installs

## Upgrade Pip
- `python -m pip install --upgrade pip setuptools wheel`
## Serial coms

- `python -m pip install pyserial`
- `python -m pip install drawnow`
- `python -m pip install pyfirmata`

## GUI 
- `python -m pip install wxPython`